apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile project(':bytecode-processor')

    compileOnly "com.android.tools.build:gradle-api:2.1.0"
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
